# database
spring.sql.init.platform=h2
spring.datasource.url=jdbc:h2:mem:testdb;MODE=PostgreSQL;IGNORECASE=TRUE
spring.h2.console.enabled=true

# Internationalization
spring.messages.basename=messages/messages

# Actuator
management.endpoints.web.exposure.include=*

# Logging
logging.level.sql=debug

# Maximum time static resources should be cached
spring.web.resources.cache.cachecontrol.max-age=12h

spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB

#TODO Please customize properties below in an application-default.properties file (already ignored by git on purpose)
#resize.function.url=http://localhost:8081/process
